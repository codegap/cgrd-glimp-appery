/*
 * JS for startScreen generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'bb459362-af90-40c0-84be-fb89d688cc52';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "detail",
    "location": "detail.html"
}, {
    "name": "startScreen",
    "location": "startScreen.html"
}];

startScreen_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'glimp_logo': 'startScreen_glimp_logo',
        'editie_label': 'startScreen_editie_label',
        'peer_label': 'startScreen_peer_label',
        'glimp_list': 'startScreen_glimp_list',
        'li_5': 'startScreen_li_5',
        'mobilelistitembutton_21': 'startScreen_mobilelistitembutton_21',
        'li_6': 'startScreen_li_6',
        'mobilelistitembutton_23': 'startScreen_mobilelistitembutton_23',
        'li_7': 'startScreen_li_7',
        'mobilelistitembutton_25': 'startScreen_mobilelistitembutton_25',
        'li_8': 'startScreen_li_8',
        'mobilelistitembutton_27': 'startScreen_mobilelistitembutton_27'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=page] div[data-role=footer]").css("bottom", "-36px");
    }

    if (Appery.getTargetPlatform == "I") {
        $.each(window.n2id, function(name, id) {
            var elApperyRole = $("#" + id).attr("data-role");
            if (elApperyRole != "appery_label" && elApperyRole != "appery_link") {
                var appleDataSelector = "#" + id + "[x-apple-data-detectors], #" + id + " [x-apple-data-detectors]";
                $(appleDataSelector).attr("x-apple-data-detectors", "false");
            }
        });
    }

    Appery.CurrentScreen = 'startScreen';

    /*
     * Nonvisual components
     */
    var datasources = [];

    /*
     * Events and handlers
     */

    // Before Show
    startScreen_beforeshow = function() {
        Appery.CurrentScreen = "startScreen";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_3671_onLoad = startScreen_onLoad = function() {
        screen_3671_elementsExtraJS();
        Appery('glimp_logo').css('margin', 'auto');
        Appery('glimp_logo').css('display', 'block');;

        // TODO fire device events only if necessary (with JS logic)
        startScreen_deviceEvents();
        startScreen_windowEvents();
        screen_3671_elementsEvents();
    }

    // screen window events
    screen_3671_windowEvents = startScreen_windowEvents = function() {

        $('#startScreen').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    startScreen_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_3671_elementsExtraJS = startScreen_elementsExtraJS = function() {
        // screen (startScreen) extra code

        /* glimp_list */

        listView = $("#startScreen_glimp_list");
        theme = listView.attr("data-theme");
        if (typeof theme !== 'undefined') {
            var themeClass = "ui-btn-up-" + theme;
            listItem = $("#startScreen_glimp_list .ui-li-static");
            $.each(listItem, function(index, value) {
                $(this).addClass(themeClass);
            });
        }

        /* li_5 */

        /* li_6 */

        /* li_7 */

        /* li_8 */

    }

    // screen elements handler
    screen_3671_elementsEvents = startScreen_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

        $('#startScreen_mobilecontainer1 [name="li_5"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    localStorage.setItem('imgname', 'files/views/assets/image/glimp-5-320x480.png');
                    Appery.navigateTo('detail', {
                        reverse: false
                    });

                }
            },
        });

        $('#startScreen_mobilecontainer1 [name="li_6"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    localStorage.setItem('imgname', 'files/views/assets/image/glimp-6-320x480.png');
                    Appery.navigateTo('detail', {
                        reverse: false
                    });

                }
            },
        });

        $('#startScreen_mobilecontainer1 [name="li_7"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    localStorage.setItem('imgname', 'files/views/assets/image/glimp-7-320x480.png');
                    Appery.navigateTo('detail', {
                        reverse: false
                    });

                }
            },
        });

        $('#startScreen_mobilecontainer1 [name="li_8"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    localStorage.setItem('imgname', 'files/views/assets/image/glimp-8-320x480.png');
                    Appery.navigateTo('detail', {
                        reverse: false
                    });

                }
            },
        });

    }

    $("#startScreen").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        startScreen_beforeshow();
    });

    if (runBeforeShow) {
        startScreen_beforeshow();
    } else {
        startScreen_onLoad();
    }

}

$("#startScreen").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    startScreen_js();
});